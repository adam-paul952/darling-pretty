// import { Router } from "react-router-dom";
// import { createMemoryHistory } from "history";

// import { render, screen } from "@testing-library/react";
// import { testSession } from "../../util/testUtil";

// import { SessionInfo } from "../SessionDescription";

// describe("<PictureDescription />", () => {
//   const setSessionDate = jest.fn();
//   const history = createMemoryHistory();
//   it("should render a card for test session", () => {
//     render(
//       <SessionInfo session={testSession} setSessionDate={setSessionDate} />
//     );
//     expect(screen.getByText("Price")).toBeInTheDocument();
//     expect(screen.getByText(testSession.price)).toBeInTheDocument();
//     expect(screen.getByText("Date")).toBeInTheDocument();
//     expect(screen.getByText(testSession.date)).toBeInTheDocument();
//   });
// });
